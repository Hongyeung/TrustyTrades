<%= form_for [@user, @general_review] do |f| %>
  <% if @general_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@general_review.errors.count, "error") %> prohibited this general_review from being saved:</h2>

      <ul>
      <% @general_review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rating %><br>
    <%= f.number_field :rating %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
